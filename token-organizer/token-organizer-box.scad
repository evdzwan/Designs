include <token-organizer-box-module.scad>
$fn = 50;

sizeX = 100;
sizeY = 100;
sizeZ = 30;
spacing = .4;
thickness = 2;
radius = 4;
inset = 4;

renderBox(sizeX, sizeY, sizeZ, radius, thickness, inset, spacing);
